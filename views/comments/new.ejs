<!--new comment-->

<% include ../partials/header %>
    <div class="container">
        <h1 class="form-heading">Add a comment to <%= campground.name %></h1>
        <div class="row">
            <div style="width:40%; margin:0 auto;">
            <form action="/campgrounds/<%= campground._id %>/comments" method="POST">
                <div class="form-group">
                    <textarea class="form-control" name="comment[text]">Write your comments here</textarea>
                </div>
                <button class="btn btn-primary btn-block">SUBMIT</button>
            </form>
            <a style="display:block; margin-top:7px;"href="/campgrounds/<%= campground._id %>">Go back</a>
            </div>
       </div>
    </div>
<% include ../partials/footer %>
